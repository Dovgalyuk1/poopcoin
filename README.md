# POOP ($POOP)

Одностраничный сайт: хромовая какашка = финансовая пирамида из трёх этажей.
Статика без сборки — `index.html` + `assets/`. Деплой: залить папку на GitHub → импорт в Vercel, build command не нужен.
Контракт и ссылку на X вписывать в блоке SETTINGS в самом верху `index.html`.
